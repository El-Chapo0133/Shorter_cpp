#ifndef DEF_SORTER
#define DEF_SORTER

class Sorter {
	public:
		int sortArray(int array[]);
		int findValueGetIndex(int array[], int value);
		bool findValueGetBool(int array[], int value);
	private:
}

#endif // !DEF_SORTER